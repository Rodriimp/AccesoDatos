package proyecto.services;

import java.sql.Connection;
import java.sql.SQLException;

import proyecto.dao.UsuariosDao;
import proyecto.modelo.Usuario;

public class UsuariosService {
	private OpenConnection connProvider;

	public UsuariosService() {
		this.connProvider = new OpenConnection();
	}

	public Usuario login(String email, String pass) throws FctException {
		Connection conn = null;
		Usuario u = new Usuario();
		try {
			conn = connProvider.abrirConexion();
			UsuariosDao userDao = new UsuariosDao();
			u = userDao.consultarUsuario(conn, email);
			
			if()

		} catch (Exception e) {
			throw new FctException("Error al obtener pagos de clientes de la bbdd", e);

		} finally {
			try {
				conn.close();
			} catch (SQLException e) {
			}
		}
		return null;
	}
}
